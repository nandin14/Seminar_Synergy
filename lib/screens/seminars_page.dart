import 'package:flutter/material.dart';

class SeminarsPage extends StatelessWidget {
  const SeminarsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Seminars Page'),
    );
  }
}
